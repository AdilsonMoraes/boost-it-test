version: "3.1"

volumes:
  mssql-developer: {}

services:
  api:
    container_name: api
    image: api
    build:
      context: .
      dockerfile: ./Npf.Process.Selections.Api/Dockerfile
    ports:
      - "8080:80"
    networks:
      - network
 
networks:
  network:
    driver: bridge
